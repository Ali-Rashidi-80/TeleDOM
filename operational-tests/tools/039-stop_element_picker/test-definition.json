{
  "tool": "stop_element_picker",
  "category": "live_browser_control",
  "executionMode": "live",
  "schema": {
    "type": "object",
    "properties": {
      "tabId": {
        "type": "number",
        "description": "Optional target Chrome tab ID (defaults to currently active tab)"
      }
    }
  },
  "expectedBehavior": "Deactivates element picker mode"
}