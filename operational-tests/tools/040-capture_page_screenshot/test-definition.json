{
  "tool": "capture_page_screenshot",
  "category": "live_browser_control",
  "executionMode": "live",
  "schema": {
    "type": "object",
    "properties": {
      "tabId": {
        "type": "number",
        "description": "Optional target Chrome tab ID (defaults to currently active tab)"
      },
      "format": {
        "type": "string",
        "enum": [
          "png",
          "jpeg"
        ],
        "description": "Image format (default: png)"
      },
      "outputPath": {
        "type": "string",
        "description": "Optional file path to save decoded PNG/JPEG image directly to disk"
      }
    }
  },
  "expectedBehavior": "Captures full page visible screenshot dataUrl with metadata"
}