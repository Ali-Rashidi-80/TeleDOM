{
  "tool": "compare_page_states",
  "category": "historical",
  "executionMode": "historical",
  "schema": {
    "type": "object",
    "properties": {
      "snapshotIdA": {
        "type": "string",
        "description": "First snapshot id (default: second-most-recent)"
      },
      "snapshotIdB": {
        "type": "string",
        "description": "Second snapshot id (default: most-recent)"
      }
    }
  },
  "expectedBehavior": "Compares two most recent snapshots"
}