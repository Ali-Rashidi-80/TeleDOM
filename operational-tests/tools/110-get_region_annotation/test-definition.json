{
  "tool": "get_region_annotation",
  "category": "historical",
  "executionMode": "historical",
  "schema": {
    "type": "object",
    "properties": {
      "projectName": {
        "type": "string"
      },
      "regionId": {
        "type": "string"
      }
    },
    "required": [
      "projectName",
      "regionId"
    ]
  },
  "expectedBehavior": "Loads the most recently captured region annotation"
}