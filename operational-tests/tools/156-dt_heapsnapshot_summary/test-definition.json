{
  "tool": "dt_heapsnapshot_summary",
  "category": "devtools_capability",
  "executionMode": "live",
  "schema": {
    "type": "object",
    "properties": {
      "snapshotId": {
        "type": "string"
      },
      "limit": {
        "type": "number",
        "description": "Max classes returned (default 50)"
      }
    },
    "required": [
      "snapshotId"
    ]
  },
  "expectedBehavior": "Parses and analyzes the heap snapshot with the real V8-format parser"
}