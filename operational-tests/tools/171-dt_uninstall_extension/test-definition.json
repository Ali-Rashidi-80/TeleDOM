{
  "tool": "dt_uninstall_extension",
  "category": "devtools_capability",
  "executionMode": "live",
  "schema": {
    "type": "object",
    "properties": {
      "extensionId": {
        "type": "string"
      }
    },
    "required": [
      "extensionId"
    ]
  },
  "expectedBehavior": "Soft-uninstalls (disables) the extension"
}