{
  "tool": "td_validate_fix",
  "category": "historical",
  "executionMode": "historical",
  "schema": {
    "type": "object",
    "properties": {
      "incidentId": {
        "type": "string",
        "description": "Incident being fixed"
      },
      "fixRef": {
        "type": "string",
        "description": "Fix reference"
      }
    },
    "required": [
      "incidentId",
      "fixRef"
    ]
  },
  "expectedBehavior": "Returns a structured v4 intelligence result with honest status taxonomy (PASS/INCONCLUSIVE/DEGRADED/UNSUPPORTED)"
}